<%= form_with(model: recofriendation, local: true) do |form| %>
  <% if recofriendation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recofriendation.errors.count, "error") %> prohibited this recofriendation from being saved:</h2>

      <ul>
        <% recofriendation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recofriendation %>
    <%= form.text_field :recofriendation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
